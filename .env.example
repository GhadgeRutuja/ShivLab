# Example environment variables for local testing or deployment
# Copy this file to .env (do NOT commit your real .env to git)

EMAIL_ADDRESS=shivghadelab@gmail.com
# Use a Gmail App Password or SMTP password. Create an app password via Google Account -> Security -> App passwords
# Replace the value below with your app password. DO NOT commit real secrets.
EMAIL_PASS=your_gmail_app_password_here

# When running locally with Vercel CLI (vercel dev) these will be loaded.